#[allow(unused_imports)]
use cps::debug::*;
use proconio::input;

fn solve() {
    input! {
        n: u64,
    }
}

fn main() {
    input!{
        t: usize,
    }
    for _ in 0..t {
        solve();
    }
}

