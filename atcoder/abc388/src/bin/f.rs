#[allow(unused_imports)]
use cps::debug::*;
use proconio::input;

fn main() {
    input!{
        n: usize,
        m: usize,
        a: usize,
        b: usize,
        mut w: [(u64, u64); m],
    }
}

